import 'package:flutter/material.dart';
import 'package:gap/gap.dart';

const miniGap = Gap(4);
const smallGap = Gap(8);
const mediumGap = Gap(16);
const bigGap = Gap(32);
const largeGap = Gap(64);
const gridSpacing = 15.0;
const gridRunSpacing = 10.0;
const kPagePadding = EdgeInsets.symmetric(horizontal: 16, vertical: 12);
